- model_class = Contest
.page-header
  %h1=t '.title', default: model_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:title)
      %th= model_class.human_attribute_name(:description)
      %th= model_class.human_attribute_name(:link)
      %th= model_class.human_attribute_name(:grant_operator)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', default: t("helpers.actions")
  %tbody
    - @contests.each do |contest|
      %tr
        %td= link_to contest.id, edit_admin_contest_path(contest)
        %td= contest.title
        %td= contest.description
        %td= link_to contest.link, contest.link
        %td= contest.grant_operator.title
        %td=l contest.created_at
        %td
          = link_to t('.edit', default: t("helpers.links.edit")), edit_admin_contest_path(contest), class: 'btn btn-mini'
          = link_to t('.destroy', default: t("helpers.links.destroy")), admin_contest_path(contest), method: :delete, data: { confirm: t('.confirm', default: t("helpers.links.confirm", default: 'Are you sure?')) }, class: 'btn btn-mini btn-danger'

= link_to t('.new', default: t("helpers.links.new")), new_admin_contest_path, class: 'btn btn-primary'
